# Отчёт — лабораторная работа 1, построение витрины данных. Рахматуллин Камиль Айратович БВТ2403
## 1. Разведка
Строки  
clients: 12204;  
transactions: 2321803;  
accounts: 18995;  
app_events: 554745;  
cards: 16660;  
mcc_codes: 25;  
merchants: 349;  
rates: 66;  
regions: 24;  
Подозрительные колонки: transactions и merchants связаны друг с другом и обе содержат mcc и channels, что может вызвать конфликт значений.
## 2. Чек-лист
2.1 Грязные справочные значения  

### Пол
Запрос: select gender, count(\*) from raw.clients group by gender  
Лишних способов написания: 6  
Запрос: select count(\*) from raw.clients where gender not in ('М', 'Ж')  
Строк затронуто: 2465 исползуют другое написание помимо М или Ж  

### Города
Запрос: select city from raw.clients where city like '%г. %' or city = upper(city) or city = lower(city)  
Лишних способов написания: 61  
Строк затронуто: 331  

### Сегмент
Запрос: select segment, count(\*) from raw.clients group by 1 order by 2 desc  
Лишних способов написания: 3  
Строк затронуто: 2480  

Грозит: Без нормализации в dim_client будут лишние отделные группы при использовании group by.

2.2 Два формата даты в одной колонке 
Запросы:  
select count(*) from raw.clients where birth_date like '%.%';  
Результат: 4792  
select count(*) from raw.clients where birth_date like '%-%';  
Результат: 7412  

Грозит: колонка текстовая, поэтому может выполняться неверное сравнение дат  

2.3 Полные дубли клиентов
Запрос: select count(\*) as duplicated_client_ids from ( select client_id from raw.clients group by client_id having count(\*) > 1)  
Строк затронуто: 120  

Грозит: неверный результат агрегатных функций, двоятся операции join с дупликатами строк  
2.4 Дубли транзакций
Запрос: select txn_id, count(\*) from raw.transactions group by txn_id having count(\*) > 1  
Строк затронуто: 9197  

Грозит: неверный результат агрегатных функций  

2.5 Отказы и возвраты
Запрос: select amount from raw.transactions where status = 'reversed' limit 5;  
Грозит: если считать оборот по всем статусам подряд (а не только approved), отменённые и возвращённые операции задвоят или обнулят реальную сумму покупок  

2.6 Дырявый календарь курсов
Запрос: select count(\*) from raw.rates where date between '2026-07-01' and '2026-07-31';  
Строк затронуто: 8  
Грозит: транзакции в выходные не используют корректный курс  
## 3. Зерно 
Вариант б, потому что дубликаты искажают агрегатные функции, такие как COUNT и SUM. Помимо этого нас интересуют транзакции всех типов в целях аналитики.  
Р1: Да, попадают. Но в этом случае при использовании SUM следует добавить WHERE status == ‘approved’, чтобы сумма не была завышена операциями declined или занижена операциями reversed.;  
Р2: По правилам ЦБ используется курс опубликованный в пятницу. JOIN по rates.date это ошибка так как rates не содержит запись курса по выходным дням, хотя транзакции могут выполняться в субботу и воскресенье.;  
## 4. Самопроверка
count(\*) = 2312606; client_sk=-1: 11557; 
пункт 2.4 починила конструкция qualify row_number() over (partition by txn_id order by txn_ts) = 1  
## 5. 
Q1:  
Запрос: select  
    dc.federal_district,  
    sum(f.amount_rub) as turnover_rub  
from marts.fct_transactions f  
join marts.dim_client dc on dc.client_sk = f.client_sk  
join marts.dim_date d    on d.date_sk = f.date_sk  
where f.status = 'approved'  
  and f.channel in ('pos', 'ecom')  
  and d.date_actual >= date '2026-07-01' and d.date_actual < date '2026-08-01'  
group by dc.federal_district  
order by turnover_rub desc;  
| federal_district | turnover_rub |
|---|---|
| ЦФО | 366 053 439.05 |
| ПФО | 240 743 911.89 |
| СЗФО | 153 173 350.76 |
| ЮФО | 114 976 832.59 |
| СФО | 105 044 029.50 |
| УФО | 78 549 108.39 |
| ДФО | 21 482 725.60 |
| Не определён | 17 200 149.35 |
| СКФО | 9 364 525.36 |

Сырой запрос:  
select  
    reg.federal_district,  
    sum(t.amount) as turnover_rub   
from raw.transactions t  
join raw.cards k      on k.card_id    = t.card_id  
join raw.accounts a   on a.account_id = k.account_id  
join raw.clients c    on c.client_id  = a.client_id  
join raw.regions reg  on reg.city     = c.city  
where t.status = 'approved'  
  and t.channel in ('pos', 'ecom')  
  and t.txn_ts::date between date '2026-07-01' and date '2026-07-31'  
group by reg.federal_district   
order by turnover_rub desc;  
| federal_district | turnover_rub |
|---|---|
| ЦФО | 344 353 916.98 |
| ПФО | 226 548 416.54 |
| СЗФО | 144 847 467.81 |
| ЮФО | 109 086 081.80 |
| СФО | 98 836 496.71 |
| УФО | 74 468 743.65 |
| ДФО | 20 853 331.91 |
| СКФО | 8 403 315.13 |

Витрина исключает дубли строк, поэтому суммы ниже, чем в сыром запросе. amount_rub в витрине пересчитан по курсу на дату через ASOF JOIN, а сырой запрос не конвертирует валюту вовсе. Операции, где карта или счёт не найдены в АБС, в витрине не теряются, а попадают на client_sk = -1.  

Q2:  
select  
    dm.merchant_name,  
    sum(f.amount_rub) as turnover_rub  
from marts.fct_transactions f  
join marts.dim_client dc   on dc.client_sk   = f.client_sk  
join marts.dim_merchant dm on dm.merchant_sk = f.merchant_sk  
where dc.segment = 'premium'  
  and f.status   = 'approved'  
group by dm.merchant_name  
order by turnover_rub desc  
limit 5;  

| merchant_name | turnover_rub |
|---|---|
| Перевод СБП | 216 998 467.21 |
| Снятие наличных | 178 080 900.00 |
| РЖД | 18 415 442.25 |
| Аэрофлот | 16 880 290.13 |
| Wildberries | 15 595 011.45 |

Финальный вопрос: Я бы выбрал звезду Кимбалла.

В банке уже имеется фиксированная структура и скорее всего нет потребности отслеживать предыдущие состояния таблиц, поэтому усилия для создания Data Vault не окупаются. Для аналитики с понятным набором бизнес-вопросов это избыточная инвестиция. Также даже при наличии сырых данных нужно построить еще один нормализованный слой данных, что займет слишком много времени, поэтому Инмон менее эффективен. Запросы направлены в основном только на транзакции, следовательно благодаря звезде они получаются простыми и понятными аналитикам, быстро считаются за счёт денормализации.

